package test;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import solver.Sudoku;

class SudokuTest {
	//any class variables the below tests need

	@BeforeEach
	void setUp() throws Exception {
		// create things that may be shared among the tests below so you don't have to create them for each test
	}

	@Test
	void testSudoku() {
		// create sudoku instances and assert that the instances are created and fail as expected
	}

	@Test
	void testValidateBoardSize() {
		// test the validation method to make sure it works as expected
	}

}
